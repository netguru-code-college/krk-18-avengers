<%= form_for @event do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :price %>
  <%= f.number :price %>
  <%= f.fields_for :users_products do |up|%>
    <%= up.select(:user_ids, @users, {}, {multiple: true})%>
  <% end %>
  <%= f.submit %>
<% end %>
